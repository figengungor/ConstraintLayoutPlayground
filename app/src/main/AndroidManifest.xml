<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.figengungor.constraintlayoutplayground">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConstraintsActivity" />
        <activity android:name=".ChainsActivity" />
        <activity android:name=".GuidelinesActivity" />
        <activity android:name=".DimensionsActivity" />
        <activity android:name=".BarriersActivity" />
        <activity android:name=".BackgroundActivity" />
        <activity android:name=".LinearLayoutActivity" />
        <activity android:name=".PercentLayoutActivity" />
        <activity android:name=".RelativeLayoutActivity" />
        <activity android:name=".FreeStyleActivity" />
        <activity android:name=".CentringActivity" />
        <activity android:name=".ConstraintSetActivity"/>
    </application>

</manifest>